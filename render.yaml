services:
  - type: web
    name: edgerift
    runtime: node
    buildCommand: npm install
    startCommand: node --max-old-space-size=450 server/index.cjs
    envVars:
      - key: NODE_ENV
        value: production
      - key: JWT_SECRET
        generateValue: true
      - key: ALLOWED_ORIGINS
        value: https://edgerift.vercel.app
    healthCheckPath: /api/me
